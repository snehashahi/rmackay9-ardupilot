/*
 *  N dimensional matrix operations
 */

#pragma once

#include "math.h"
#include <stdint.h>
#include "matrixN.h"

/*
template <typename T, uint8_t N>
class Matrix4f : public MatrixN<float, 4>
{

public:

    // calculate the inverse of this matrix
    // results stored in inv
    // returns true if this matrix is invertible, false otherwise and inv is unmodified
    //bool inverse(Matrix4f& inv) const override { return false; }
    // calculate the inverse of this matrix
    // results stored in inv
    // returns true if this matrix is invertible, false otherwise and inv is unmodified
    //virtual bool inverse(MatrixN<T,N>& inv) const { return false; }

};
*/
